node_modules
dist
.astro

# Lockfile no versionado: se generó en Windows y omite dependencias
# opcionales de Linux (@emnapi/*), lo que rompe `npm ci` en Cloudflare.
# Sin lockfile, Cloudflare usa `npm install` y resuelve por plataforma.
package-lock.json
